<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.denisprawira.ta">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".User.Helper.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".User.UI.ProfileEditActivity"></activity>
        <activity android:name=".User.UI.SearchUserActivity"></activity>
        <activity android:name=".User.UI.SearchUserDatabaseActivity"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".User.UI.Event.EventMapsActivity"
            android:label="@string/title_activity_event_maps" />
        <activity android:name=".User.UI.PhotoViewerActivity" />
        <activity android:name=".User.UI.Event.EventRequestEditActivity" />
        <activity android:name=".User.UI.Event.DetailEventRequestActivity" />
        <activity android:name=".User.UI.Event.EventRequestUnapprovedActivity" />
        <activity android:name=".User.UI.DonorJoinedDetailActivity" />
        <activity android:name=".User.UI.Event.DetailDonorRequestActivity" />
        <activity android:name=".User.UI.FindDonorActivity" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <service
            android:name=".User.Services.FirebaseServices"
            android:stopWithTask="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity android:name=".User.UI.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".User.UI.LoginActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme"/>
        <activity
            android:name=".User.UI.HomeActivity"
            android:label="@string/title_activity_home"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".User.UI.Event.EventActivity" />
        <activity
            android:name=".User.UI.HelpMapsActivity"
            android:label="@string/title_activity_help_maps" />
        <activity
            android:name=".User.UI.MapsActivity"
            android:label="@string/title_activity_help_maps" />
        <activity android:name=".User.UI.ChatRoomActivity" />
        <activity
            android:name=".User.UI.CallUserActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_call_user"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".User.UI.Event.EventDetailActivity" />
        <activity android:name=".User.UI.JoinedEventDonorActivity" />
        <activity
            android:name=".User.UI.RequestEventDonorActivity"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme" />
        <activity android:name=".User.UI.BloodBankActivity" />
        <activity android:name=".User.UI.ReminderActivity" />
        <activity android:name=".User.UI.ReminderEditActivity" />

        <receiver android:name=".User.Helper.AlarmReceiver" />
        <receiver android:name=".User.Helper.BootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>